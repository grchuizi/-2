# 换电脑安装指南（Setup on a new machine）

> 🔑 **API Key 是每个人自己的——仓库里没有任何人的 key。** 本仓库（含全部 git 历史）不包含 DeepSeek / Server酱 密钥；每个使用者都必须用自己的 key，去各自官网申请后填到自己的 Secrets / 环境变量里，见下文「配 API Key」。

> 先看清一件事：**自动工作流（定时触发 + 运行 + auto-commit + 微信推送）跑在 GitHub Actions 云端，不绑任何一台电脑。**
> 新电脑上真正要做的，只有「本地开发 / 手动跑」的环境 + git 权限。

---

## 一图总结：什么在本机、什么在云端

| 东西 | 在哪 | 换电脑要不要重做 |
|---|---|---|
| 定时触发 + 运行 + auto-commit + 微信推送 | GitHub 云端（Actions） | ❌ 不用，自动照跑 |
| 代码 | GitHub 仓库 | ❌ `git clone` 即可 |
| GitHub Secrets | 仓库 Settings | 同 repo 不用；换 repo 要重填 |
| git 登录凭据 | 本机 | ✅ 每台电脑配一次 |
| Python + venv + 依赖 | 本机 | ✅ 每台电脑装一次（有脚本） |
| API Key 环境变量 | 本机临时变量 | ✅ 每台电脑设一次 |

---

## 场景一：同一个 GitHub 仓库，换一台新电脑（最常见）

### 第 0 步 — 装前置软件

1. **Git for Windows**：https://git-scm.com/download/win （一路默认）
2. **Python 3.12**（3.11+ 也行）：https://www.python.org/downloads/ —— 安装时勾选 **"Add Python to PATH"**

### 第 1 步 — 拉代码（私有仓库，需先授权）

```powershell
git clone https://github.com/grchuizi/-2.git
cd -2
```

> 私有仓库：第一次会弹 GitHub 登录（或用 `gh auth login` / Personal Access Token）。登录凭据只在这台电脑配一次。

### 第 2 步 — 建隔离环境 + 装依赖

```powershell
# 用项目自带脚本建 venv（也可手动 python -m venv .venv）
powershell -ExecutionPolicy Bypass -File .\scripts\01_Create-Isolated-Venv.ps1

# 装包 + 开发依赖（测试 / lint 用）
.venv\Scripts\pip install -e ".[dev]"
```

### 第 3 步 — 配 API Key（临时环境变量，绝不写进文件）

```powershell
$env:DEEPSEEK_API_KEY = "你的 DeepSeek Key"
$env:SERVERCHAN_KEY    = "你的 Server酱 SendKey"
```

**去哪申请（都是你自己的账号，跟作者无关）：**
- **DeepSeek**：https://platform.deepseek.com → 注册登录 → 左侧「API Keys」→ 创建，复制 `sk-` 开头的 key。
- **Server酱**：https://sct.ftqq.com → 用微信扫码登录 → 拿到 `SCT` 开头的 SendKey。

> 两个环境变量名是 `DEEPSEEK_API_KEY` 和 `SERVERCHAN_KEY`（`.env.example` 里的 `SERVERCHAN_SENDKEY` 是旧名，别用错）。
> 真实密钥严禁写入任何项目文件，也不提交 Git。

### 第 4 步 — 验证装好了

```powershell
.venv\Scripts\python main.py --version     # 应显示当前版本号
.venv\Scripts\python main.py --validate    # 应显示 Validation OK
.venv\Scripts\python -m pytest             # （可选）跑测试
```

### 第 5 步 —（可选）本地手动跑一次完整链路

```powershell
.venv\Scripts\python main.py --topic charging_pile --task charging_cn_weekly --phase2 --phase3 --phase4 --notify true
```

---

## 场景二：换到新的 GitHub 账号 / 新仓库（fork 一份）

代码 clone 过来后，多两步：**先启用 Actions，再填两个 Secrets**（顺序很重要，Actions 不启用的话 Secrets 填了也跑不了）。

### 第 1 步 — 启用 Actions（fork 默认是冻结的）

GitHub 对 fork 到新仓库的 Actions **默认禁用**（安全策略）。不启用的话，Actions 页看不到 `Run workflow`、所有 workflow 灰色。在你**自己的 fork 仓库**里：

1. `Settings → Actions → General → Actions permissions` → 选 **"Allow all actions and reusable workflows"**。
2. 同一页 `Workflow permissions` → 选 **"Read and write permissions"**（本仓库 workflow 要 `contents: write` 来自动提交数据）。
3. 回 `Actions` 页签 → 左列找到 `manual_run.yml` → 点进去 → 右上角 **"Enable workflow"**。
4. 此时 `Actions → Manual Run → Run workflow` 按钮才会出现。

> ⚠️ fork 后 **`scheduled_dispatcher.yml` 的 cron 不会自动生效**——要等上面第 1 步启用 Actions 后才会跑。

### 第 2 步 — 填两个 Secrets（配在**你**的 fork 仓库，不是原仓库）

1. 仓库 `Settings → Secrets and variables → Actions → New repository secret`
2. 加两个（值用你自己申请的 key，去哪申请见上文「第 3 步」）：
   - `DEEPSEEK_API_KEY`
   - `SERVERCHAN_KEY`
3. 注意变量名是 **`SERVERCHAN_KEY`**（不是 `.env.example` 里旧的 `SERVERCHAN_SENDKEY`，配错会静默失败）。
4. 完事 —— 启用 Actions 后，cron 每天北京 08:17 自动跑；也可手动 `Actions → Manual Run → Run workflow` 触发一次验证。

---

## 常见问题

- **不设 API Key 能不能跑？** 能，但会降级：没有 `DEEPSEEK_API_KEY` 时 LLM 分析 / 热点发现退化为空，检索回退固定三族查询；没有 `SERVERCHAN_KEY` 时不推微信。
- **换关键词 / 换行业？** 见 [`如何换关键词.md`](如何换关键词.md)。
- **同一天要强制重跑？** 手动触发 `manual_run.yml`（`force: true`），或本地 `python scheduler.py --run-due --force`。
